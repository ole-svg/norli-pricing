{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -c \"from fastapi import FastAPI; import uvicorn; app = FastAPI(); app.get('/health')(lambda: {'status':'ok'}); uvicorn.run(app, host='0.0.0.0', port=8080)\"",
    "restartPolicyType": "ON_FAILURE"
  }
}
